services:
  - type: web
    name: coldmail-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        value: coldmail-secret-2026
      - key: PYTHON_VERSION
        value: 3.11.0